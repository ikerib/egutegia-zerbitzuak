{% extends 'base.html.twig' %}

{% block body %}
    <h1>Azpisaila</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ azpisaila.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ azpisaila.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_azpisaila_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_azpisaila_edit', { 'id': azpisaila.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
