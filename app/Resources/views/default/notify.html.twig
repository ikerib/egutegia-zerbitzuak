{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">{{message.name}}</h3>
                </div>
                <div class="panel-body">
                    {{message.description | raw}}
                </div>
            </div>
        </div>
        <div class="col-md-1"></div>
    </div>

    <div class="row">
        <div class="col-md-12 text-center">
            <a class="btn btn-default" href="{{ path('user_notifycation_readed', {'id': message.id}) }}">Onartu eta markatu irakurrita moduan.</a>
        </div>
    </div>

{% endblock %}
