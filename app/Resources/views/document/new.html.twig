{{ form_start(form, {'attr': {'id': 'frmFile'}}) }}

    <div class="form-group">
        <label for="appbundle_document[uploadfile][file]" class="col-sm-3 control-label">{{ 'Aukeratu Dokumentua' | trans }}</label>
        <div class="col-sm-8">
            {{ form_widget(form.uploadfile) }}
        </div>
    </div>
    <div class="form-group">
        <label for="appbundle_document_orden" class="col-sm-3 control-label">{{ 'Orden' | trans }}</label>
        <div class="col-sm-8">
            {{ form_widget(form.orden) }}
        </div>
    </div>

    <div style="display: none;">
        {{  form_rest(form) }}
    </div>


{{ form_end(form) }}
