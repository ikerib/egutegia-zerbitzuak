{% extends 'base.html.twig' %}

{% block body %}
    <h1>Saila</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saila.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ saila.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_saila_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_saila_edit', { 'id': saila.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
