<form method="post" action="{{ path('admin_message_delete', {'id': message.id}) }}" onSubmit="return confirm('Ziur zaude?')">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button class="btn btn-sm btnRemoveRow text-danger" ><i class="fa fa-trash text-danger" aria-hidden="true"></i></button>
</form>
